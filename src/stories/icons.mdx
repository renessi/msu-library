import { Meta, Title, IconGallery, IconItem} from "@storybook/blocks";
import * as Icons from "../stories/Svg.stories.js"

<Meta title="System/Icons"/>


<IconGallery>
  {Object.entries(Icons)
    .filter(([name]) => name !== "iconList")
    .map(([name, icon]) => (
        <IconItem key={name} name={name}>
        </IconItem>
    ))}
</IconGallery>